# 스프린트 7 — 제품 라이브러리 · Customize · 드롭다운

요청 여섯 가지. ①② 드롭다운, ③ Customize + 제품 스펙/데이터시트, ④ 벤더별 기본값,
⑤ Qcells AC 결합, ⑥ 그래프를 위로.

## 리서치 조건을 먼저 밝힌다

이 환경의 네트워크 정책이 제조사 사이트 접근을 막는다(프록시 403). 그래서
**데이터시트 PDF 원문을 열지 못했다.** 대신 공식 도메인(enphase.com, energylibrary.tesla.com,
knowledge-center.solaredge.com, us.qcells.com)의 문서 목록과 검색 결과에 노출된 표기 값만
옮기고, 각 `sources`에 원문 URL·확인일·"PDF 원문 열람 차단, 원문 대조 필요"를 함께 적었다.
스니펫에 나오지 않은 값은 **채우지 않았다** — 예를 들어 Q.TRON 모듈의 Vmp/Imp/Voc/Isc다.

그 결과가 화면에 그대로 나타난다: Q.TRON 모듈을 고르면 DC 전압·전류가 계산되지 않고
"모듈 전기 정격이 없어 계산할 수 없다"가 뜬다. 값을 지어냈다면 그럴듯한 그래프가
나왔을 것이다. 지금 상태가 데이터의 실제 상태다.

## 확인한 값 (공식 도메인 표기 기준, 2026-09-03)

| 제품 | 값 |
|---|---|
| Enphase IQ8+/IQ8M/IQ8A | 연속 290 / 325 / 349 VA, 피크 300 / 330 / 366 VA, CEC 97.5%(8M) · 97%(8A) |
| Enphase IQ Battery 5P | 5.0 kWh · 연속 3.84 kVA · 피크 7.68 kW(3s) / 6.14 kW(10s), 내장 IQ8D-BAT 6대 |
| Tesla Powerwall 3 | 13.5 kWh · 11.5 kW · 185 A LRA · PV 20 kW STC · MPPT 6개(60–480 V) |
| SolarEdge Home Hub SE7600H | 7600 W · 240 Vac · 최대 연속 32 A. 축전지 3대/포트 2개/합계 12.5 kW |
| SolarEdge Home Battery 400V | 9.7 kWh, 인버터당 3대까지, 왕복 효율 94.5% |
| SolarEdge Backup Interface | 서비스 200 A까지, 인버터 3대까지 |
| Qcells Q.TRON BLK M-G2+(/AC) | 415–440 Wp, 108셀 TOPCon, 효율 22.5%, PMax −0.3%/°C, Voc −0.24%/°C |
| Qcells Q.HOME CORE G3 | 13.1 kWh(HUB G3 + 2대 시 26.24), 백업 7.6 kW, LG ES LFP |
| Qcells Q.HOME COMBINER 80 G1 | PV 4계통, AC 모듈 최대 44장, 200 A CT 1쌍 |

## 판단이 갈렸던 지점

### 1. class를 마케팅 명칭이 아니라 전기 구조로 정했다

SolarEdge Home Hub는 "하이브리드/스토리지 인버터"로 팔리지만 축전지가 함체 안에 없다.
`hybrid_inverter_battery`(일체형)로 두면 축전지를 0대로 해도 야간 아일랜드가 성립한다고
계산된다 — 틀린 답이다. `string_inverter` + `dc_battery` 두 노드로 나눴고, 엔진은
"인버터에 물린 DC 축전지에 잔량이 있는가"를 보고 야간 출력을 판정한다. 테스트가 이 차이를 지킨다.

### 2. AC 모듈을 클래스로 만들었다 (Qcells)

Q.TRON AC는 변환기가 모듈 안에 있어 **DC가 함체 밖으로 나오지 않는다.** 모듈+마이크로인버터
2노드로 쪼개면 있지도 않은 DC 도체가 도면에 생긴다. `ac_module` 클래스는 DC 포트를 갖지
않으며, 그래서 이 노드에는 DC 신호가 없다 — 값이 비어 있는 것이 구조의 결과다.
요청 ⑤("모듈이 모여서 AC combiner에")가 이 모델에서 그대로 나온다.

### 3. 새 데이터가 엔진 버그를 하나 드러냈다

DC 축전지를 붙이자 야간에 PV 어레이 전체가 활선으로 계산됐다. 축전지 DC가 인버터를
**관통해** 어레이로 되돌아간 것이다. 변환기는 DC 버스바가 아니다 — 인버터를 DC→DC로
지나가는 경로를 막았다. 스프린트 6까지는 DC 소스가 PV뿐이라 드러나지 않던 결함이다.

### 4. 성립하지 않는 조합에서 화면이 죽지 않게 했다

Customize는 "축전지 DC 포트가 없는 인버터 + DC 결합 ESS" 같은 조합을 고를 수 있다.
이때 그래프 해석기가 던진다(포트 없음). 예외를 삼키지 않고 도면 자리에 무엇이 안 맞는지
그대로 띄우고, 검증 패널에도 남긴다. 고를 수 있게 두는 이상 이 처리가 필요하다.

### 5. 드롭다운으로 바꾼 이유

선택지가 제품 목록까지 늘어나자 버튼 나열이 왼쪽 열을 다 먹었다. 드롭다운이면 축 하나가
한 줄이고 고른 값이 늘 보인다. enum 축 전체에 적용했다 — 백업 구성·계통 분리 장치만
바꾸면 축마다 다른 위젯이 되어 오히려 읽기 어렵다.

### 6. 그래프를 위로, 설명을 아래로 (요청 ⑥)

노드를 고르면 오른쪽 위가 파형이고, 그 아래로 동작점 · 포트별 수치와 수식 · 설계 노트 ·
제품 스펙/데이터시트 순서다. 스펙 요약은 노드를 고르지 않았을 때도 "구성 제품" 목록으로
오른쪽에 남는다.

## 남은 것

1. **Q.TRON 모듈의 STC 전기 정격** — 데이터시트 PDF를 열 수 있는 환경에서 채워야 한다
2. 마이크로인버터의 MPPT 창·최대 DC 입력, AC 모듈 내장 변환기의 연속 출력 → 클리핑 룰
3. SolarEdge Power Optimizer를 노드로 모델링할지 결정 (지금 스트링 전압은 모듈 직렬 합 근사)
4. 분기회로당 최대 유닛 수 (Enphase·Qcells 모두 기본값에 근거가 없다)
5. 온도계수는 데이터에 들어왔지만 아직 계산에 쓰지 않는다 — 저온 Voc 룰이 다음 차례다
