# 스프린트 8 — 단자 선택 · 하루 시간축 · 격자 배치

요청 여섯 가지. ① AC 모듈을 모듈+인버터로 분리, ② 신호 지점을 제품이 아니라 단자로,
③④ 시간 슬라이더와 재생, ⑤ 지역·월 선택, ⑥ 배열을 상하로도 배치.

## 판단이 갈렸던 지점

### 1. `ac_module` 클래스를 되돌렸다

스프린트 7에서 Q.TRON AC를 한 노드로 그렸다. "DC가 함체 밖으로 나오지 않으니 DC 포트도
없어야 한다"는 논리였고, 전기적으로는 맞다. 그런데 그 결정이 **모듈의 DC 출력을 볼 수 없게**
만들었다 — 이 도구를 쓰는 이유 중 하나가 그것인데도.

모듈 노드와 마이크로인버터 노드로 나눴다. 실물이 한 함체라는 사실은 device의 `sources`와
마이크로인버터 노드 노트에 남겼다. 클래스 하나를 지우는 대신 그 지식을 데이터로 옮긴 셈이다.

### 2. 신호의 손잡이를 함체에서 단자로 옮겼다

전에는 노드를 클릭하면 그 제품의 모든 포트가 한꺼번에 나왔다. 실제로 사람이 묻는 것은
"이 도체에 얼마가 흐르는가"이고, 그 지점은 제품이 아니라 **단자**다. 도체가 제품을 떠나는
좌표를 배치기가 이미 알고 있으므로(`Layout.ports`), 거기에 점을 찍고 클릭 대상으로 삼았다.
함체를 눌러 전체를 보는 길도 남겼다 — 링크에는 `…/mi-10.ac_out`처럼 단자까지 실린다.

### 3. 하루 시간축은 어디까지 계산할 것인가

CLAUDE.md §1의 "발전량 시뮬레이션 없음"과 정면으로 부딪히는 요청이다. 선을 이렇게 그었다:

- 계산한다: 위도·월·시각 → 태양 고도 → 맑은 하늘 경사면 일사 → 그 시각의 정상상태 한 점
- 하지 않는다: **시간 적분**. 누적 발전량(kWh)을 내놓는 순간 시뮬레이터가 된다
- 하지 않는다: 기상 데이터, 구름, 음영, 온도 보정, 오염, 서머타임, 경도 보정

하루 곡선은 "시각만 바꿔 조류를 49번 다시 푼 결과를 늘어놓은 것"이지 시계열 모델이 아니다.
모델 이름과 미반영 항목을 화면에 함께 띄운다 — 그럴듯한 곡선이 실측처럼 읽히면 안 된다.

시나리오와 시각은 다른 축이다. **시나리오는 회로의 상태(활선/사선), 시각은 크기**다.
야간에는 시나리오가 `producing`이어도 일사가 0이라 PV 출력이 0이 된다.

### 4. 재생을 위해 계산을 둘로 나눴다

시각이 흐를 때마다 도면을 다시 그리면 재생이 끊긴다. 결선·시나리오·룰은 동작점과 무관하므로
구조 계산과 조류 계산을 분리했다. 재생 중에는 조류와 신호만 다시 계산된다.

### 5. 배열을 접었다 — 그리고 도체가 어레이를 관통하지 않게 했다

모듈 20장을 한 줄로 늘어놓으면 도면이 2548px였다. 이제
- 짝지어진 모듈·인버터는 **세로로 포개** 한 칸이 되고,
- 묶음(분기회로·스트링)마다 **한 행**을 쓰고, 행이 너무 길면 뱀처럼 접는다.
1538 × 922로 줄었고, 모듈 바로 아래에 그 모듈의 인버터가 온다.

접고 나니 두 가지가 새로 필요했다. 랭크마다 높이가 달라졌고(접힌 블록은 여러 행이다),
블록에서 빠져나가는 도체가 아래 행을 가로질렀다. 후자는 **우측 레인**으로 뺐다 —
실제 도면에서 홈런 배선이 어레이를 관통하지 않는 것과 같은 이유다. 랭크 사이를 잇는
도체의 가로 꺾임도 중간값이 아니라 랭크 사이의 빈 띠에서 일어나게 바꿨다.

## 남은 것

1. Q.TRON 모듈의 STC 전기 정격 — 여전히 미확인이라 그 모듈의 DC 전압·전류는 계산되지 않는다
   (출력 W는 나온다). 데이터시트 PDF를 열 수 있는 환경에서 네 값만 채우면 된다
2. 온도계수는 데이터에 있는데 아직 쓰지 않는다. 시간축이 생겼으니 셀 온도 → 출력 보정이
   다음 차례다 (기상 데이터 없이 NOCT 근사까지가 한계)
3. 지역은 도시 대표 좌표다. 실제 사이트 좌표 입력을 열지 결정
4. 방위각·경사를 사이트 조건으로 받을지 결정 (지금은 정남향·경사=위도 고정)
