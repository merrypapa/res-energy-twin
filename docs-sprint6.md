# 스프린트 6 — 배열 전개 · 구성 컴포저 · 노드 신호

요청 다섯 가지를 한 스프린트로 묶었다. 셋은 서로 맞물려 있었다 — 노드를 골라 신호를
보려면 모듈이 노드여야 하고(①), 구성을 고를 수 있으려면 결선이 옵션의 함수여야 한다(④⑤).

| 요청 | 결과 |
|---|---|
| ① 모듈 1장 · 인버터 1대씩 분리 | `pv-01…pv-20`, `mi-01…mi-20`. Enphase 45노드 46엣지 |
| ② 노드별 전력·전압·전류 + 공학적 설명 | `src/analysis/` — 전력 수지 · 신호 · 파형 · 수식 |
| ③ 노드별 설계·기능 정리 | `node-notes/` 11건 (class 10 + device 1) |
| ④ grid support / 부분 / 전체 백업 선택 | `backup_mode` 축. 값이 곧 `backup_scope` |
| ⑤ MSC·게이트웨이·확장·병렬 선택 | `mid_device` · `controller` · `*_units` 축 |

## 판단이 갈렸던 지점

### 1. 축약(count)을 버렸다

`{ device: pv-module, count: 20 }`은 도면 한 칸에 "×20"을 찍는 표기였다. 이걸 두면
노드를 고를 수 없고(어느 장인가), 트렁크 전류가 계산되지 않고(누적이 없다),
비교표의 부품 수가 거짓말을 한다. 20노드로 폈다.

대신 **결선이 진짜가 됐다.** 직렬 스트링은 모듈 사이 엣지(`chain`)로, AC 트렁크는
유닛 사이 엣지로 표현된다. 이 두 결선이 신호 설명의 근거다 — 직렬은 전압이 쌓이고,
트렁크는 전류가 쌓인다. 도면에서 그 선이 수평이라는 사실 자체가 정보다.

비용: 마이크로인버터에 `trunk_in` 포트를, PV 모듈에 `dc_in` 포트를 추가했다.
실제 함체의 커넥터 구성과 1:1이 아니다 — 모델링 목적임을 device `todos`에 적었다.

### 2. topologies/*.json을 configurations/*.yaml로 바꿨다

grid support · 부분 백업 · 전체 백업 × MID 2종 × 배터리 0~4대를 파일로 쓰면 벤더당
수십 개가 되고 서로 어긋난다. 축을 데이터로 선언하고 조합은 컴포저가 만든다.
컴포저 출력은 기존 `Topology` 스키마 그대로여서 렌더러 · 시나리오 · 룰 · 비교 엔진은
이 레이어가 생긴 것을 모른다.

프리셋 id는 기존 topology id를 그대로 물려받았다. 옵션이 프리셋과 정확히 같으면
그 id를 쓴다 — 공유된 링크가 깨지지 않는다.

### 3. 성립하지 않는 조합을 축에서 빼지 않았다

Tesla `backup_mode=none`은 PW3의 `requires_one_of`(Backup Switch 또는 Gateway)를
만족하지 못해 `E025`가 뜬다. 이 조합을 UI에서 지우면 "왜 안 되는가"라는 답이 사라진다.
고를 수 있게 두고 룰이 판정하게 했다. CLAUDE.md §1의 세 번째 질문이 그것이다.

대신 **체크인된 프리셋은 error가 없어야 한다**는 선을 지켰다. `npm run validate`는
enum 조합 18개를 전부 펴서 컴포저 결함(`C0xx`)만 error로 잡고, 성립하지 않는 조합은
`C040` 정보로 남긴다.

여기서 나온 발견 하나: **Enphase는 grid support 전용 구성이 데이터상 성립하고
Tesla는 성립하지 않는다.** 배터리를 뺀 IQ8 어레이는 MID 없이 성립하지만, PW3는
게이트웨이 없이 서지 않는다. 이 차이가 맞는지는 확인 대상이고 `todos`에 적었다.

### 4. "발전량 시뮬레이션 없음"의 경계를 다시 그었다

②를 하려면 숫자가 필요하다. 비목적(§1)을 깨지 않는 선을 이렇게 잡았다 —
**정상상태 동작점 한 점**만 계산한다. 시간 적분도, 기상 데이터도, SOC 궤적도 없다.
임피던스 · 전압 강하 · 무효전력도 풀지 않는다(부하조류 해석이 아니라 전력 수지다).
CLAUDE.md §1과 §4.1에 이 경계를 명시했다.

효율(97%)과 역률(1.0)은 **가정값**이라 device-library에 넣지 않았다. 제품 스펙이
아니기 때문이다. 화면에도 "가정값, 제품 스펙 아님"으로 나온다.

### 5. PV 모듈에만 숫자를 넣었다

신호 계산에는 모듈 전기 정격이 필요하다. `generic-pv-module-400w`에 STC 값을 넣되
`sources`에 "모델링 기준값 (특정 제품 아님)"이라고 적었다 — `generic-msp-200a`가
버스바 200A를 다루는 방식과 같다. 마이크로인버터의 연속 출력은 여전히 `null`이다.
그래서 **클리핑 판정을 하지 않는다**. 정격이 없으면 계산하지 않는 원칙을 지켰다.

### 6. `max_connections` 기본값 1을 null로 바꿨다

스트링 2개를 SolarEdge/Qcells 인버터에 물렸더니 `E024`(연결 수 초과)가 떴다.
그런데 두 제품 모두 MPPT 수가 미확인이다 — 기본값 1은 "확인하지 않았다"가
"1개만 된다"는 주장으로 굳은 것이었다. null(미확인)로 바꾸고, 한도가 확인된
포트에서만 error를 낸다. 미확인 포트에 여러 도체가 붙으면 `W026`으로 남긴다.

### 7. 배치: 20단 계단을 막았다

모듈을 노드로 편 순간 계층 배치가 20단 계단을 만들었다(각 직렬 결선이 랭크를 하나씩
먹는다). 랭크를 **그룹 단위로 축약한 그래프**에서 매기고, 그룹 안의 결선은 같은 랭크
안의 수평선으로 그린다. 그룹 구간은 랭크끼리 x를 맞춰서 모듈 i와 인버터 i가 세로로
정렬된다 — 1:1 대응이 눈에 보여야 한다.

도면이 2548px가 됐다. 폭에 맞추면 라벨을 읽을 수 없어 **실제 크기가 기본**이고
종이가 스크롤된다. 비교 모드와 모바일에서는 폭에 맞춘다.

### 8. 신호 그래프도 잉크 한 색이다

§6의 색 규칙(색은 활선/사선만)을 그래프에도 적용했다. v · i · p를 한 축에 세 색으로
겹치지 않고 세로로 쌓아 시간축을 공유한다. 위상 관계는 그대로 읽히고, 색은 늘지 않는다.

## 계산이 답하는 것과 답하지 않는 것

답한다:
- 이 지점의 P(kW) · V(V, rms 또는 DC) · I(A)와 그 근거 수식
- 순시 파형 v(t) · i(t) · p(t) — 흡수/공급이 위상으로 구분된다
- 모듈 I–V · P–V 곡선(4파라미터 지수 근사)과 MPP
- 직렬 몇 번째인지, 트렁크 상류에 몇 대가 있는지 같은 구조적 근거

답하지 않는다:
- 전압 강하, 임피던스, 무효전력, 고조파, 불평형
- 온도계수(저온 Voc 상승) — 스트링 길이 설계의 핵심인데 데이터가 없다
- 인버터 클리핑 — 마이크로인버터 정격이 `null`이다
- 전환 시간, 동기 조건 — MID는 열림/닫힘 두 상태뿐이다

## 다음에 해야 할 것

1. 모듈 온도계수(β, γ)를 스키마에 넣고 저온 Voc 룰을 만든다 — 스트링 길이 판정
2. 마이크로인버터 모델별 연속 출력·효율 확인 → 클리핑 룰
3. 분기회로당 최대 유닛 수 확인 — `branch_size` 기본 10에 근거가 없다
4. 부분 백업의 실제 제품 구성 확인 (현재 Enphase/SolarEdge/Qcells는 자리표시자 MID)
5. 부하를 노드로 모델링 — `load_shed` 시나리오가 아직 대상 없이 돈다
6. 전기 엔지니어 리뷰: 심볼 표기, 노드 노트의 조항 참조(전부 `verified: false`)
