# 스프린트 1 보고 — 렌더러 설계에서 판단이 갈린 지점

## 1. 랭크를 무엇으로 매길 것인가

계층은 **전력 엣지로만** 매긴다 (`src/render/layout.ts`). 통신 엣지는 배치에
전혀 영향을 주지 않는다. 통신선까지 랭크에 넣으면 CT 배선 하나가 계층 구조를
뒤틀어 단선도가 아니라 네트워크 그림이 된다.

최장경로 랭크만 쓰면 유입 엣지가 없는 장비(배터리, PV)가 전부 최상단으로 올라간다.
Enphase 구성에서 IQ Battery가 유틸리티 서비스와 같은 줄에 서고, 배터리 → 컬라이너
엣지가 한 랭크를 건너뛰면서 계층이 실제 결선보다 한 단 깊어 보였다. 그래서
**전원은 소비처 바로 위 랭크로 끌어내린다.** 이 규칙 하나로 Enphase가 4단에서
정상적으로 읽히는 3.5단 구조가 됐다.

랭크를 건너뛰는 엣지에는 더미 노드를 넣어 한 칸씩 끊는다. 덕분에 긴 도체가 다른
장비 위를 가로지르지 않는다 — 노드에 배경판을 깔지 않아도 되는 이유가 이것이다.

## 2. 심볼을 무엇으로 고를 것인가

`class` 하나만 본다 (`src/render/symbols.ts`). vendor도 id도 보지 않는다.
호환 표를 포트 타입으로만 판정한 스프린트 0의 결정과 같은 이유다.

이 금지를 문서가 아니라 **테스트로 강제**했다. `src/render/**` 소스에 Tesla,
Enphase, Powerwall 같은 문자열이 (주석을 제외하고) 등장하면 테스트가 깨진다.
렌더러에 벤더 분기문이 들어가는 일은 리뷰어의 집중력이 아니라 CI가 막는다.

class에 심볼이 없으면 빈 함체로 떨어진다. 비슷한 장비의 심볼로 대체하지 않는다 —
그림이 모르는 것을 아는 척하면 안 된다. 현재 10개 class 전부에 심볼이 있고,
이것도 테스트가 지킨다.

**심볼 자체는 아직 검증되지 않았다.** IEEE/ANSI 관례를 따랐다고 적었지만 원문
대조는 하지 않았다. 룰 파일의 `verified: false`와 같은 수준으로 봐야 한다.

## 3. 활선/사선을 렌더러가 계산하지 않는다

렌더러는 급전 상태를 **주입받는다.** `renderTopology(topology, devices, { energization })`,
키는 `"from.port->to.port"`, 값은 `live | dead`, 없으면 live.

스프린트 1은 계통 정상 상태만 그리므로 이 인자는 항상 비어 있다. 그럼에도 지금
넣은 이유는, 스프린트 2에서 만들 시나리오 엔진의 출력 형식을 여기서 못 박아 두면
그때 렌더러를 고칠 일이 없기 때문이다. 애니메이션이 계산 결과의 표현일 뿐이라는
CLAUDE.md §4의 원칙은 정적 렌더러에서도 똑같이 적용된다.

노드의 사선 판정은 **접속된 전력 엣지가 전부 사선일 때만** 사선이다. 노드에
별도 상태를 두지 않았다 — 상태의 진짜 소재지는 엣지다.

## 4. 통신선을 어디로 지나가게 할 것인가

세 번 갈아엎었다.

- 노드 사이를 직선으로 이으면 장비 라벨 위를 그대로 지나간다
- 우측에 전용 레인을 만들어 전부 몰면, 바로 옆 장비끼리도 도면 끝까지 갔다 돌아온다
- 최종: **함체 옆면에서 나와 상대 쪽으로 향한다.** 같은 랭크에서 이웃이면 곧장
  잇고, 아니면 랭크 아래 여백(거터)을 타고 돌아간다

통신선이 보이지 않는 노드 상자 가장자리에서 끊겨 허공에서 끝나는 문제도 여기서
같이 잡았다. 도체는 상자가 아니라 **심볼 외곽**에 붙는다 (`symbolExtent`).

## 5. 색 규칙과 보조 텍스트

"색은 활선/사선 하나만 나른다"를 지키면, 정격 요약이나 도체 라벨 같은 보조
텍스트를 회색으로 낮추는 것이 규칙 위반인지가 문제가 된다.

허용하기로 했다. 근거는 **회색 텍스트는 도체가 아니라서 사선과 혼동될 수 없다**는
것이다. 대신 보조 텍스트(`#5C574F`)와 사선(`#C3BEB3`)의 명도를 확실히 벌려
두 회색이 같은 것으로 읽히지 않게 했다. 이 판단이 틀렸다면 보조 텍스트를 잉크색
그대로 두고 크기로만 위계를 주는 쪽으로 되돌리면 된다.

## 6. 노드에 배경판을 깔지 않는다

초안에서는 각 노드 뒤에 배경색 사각형을 깔아 지나가는 선과 그리드를 가렸다.
출력을 보니 그리드가 끊긴 자리가 그대로 카드로 읽혔다 — "카드에 담지 않는다"를
정면으로 어긴 셈이다. 배경판을 걷어냈고, 대신 §1의 더미 노드가 도체를 노드 위로
지나가지 않게 보장한다. 배경판은 도체 라벨 뒤에만 남겼다.

## 7. 그림이 드러낸 것

검증기가 잡지 못하는 종류의 문제들이다.

- **PV 20장과 마이크로인버터 20대가 노드 하나로 뭉쳐 그려진다.** `count: 20`으로
  뭉갠 스프린트 0의 미결정 사항이 그림에서는 숨겨지지 않는다. 스트링 구성을
  별도 구조로 뺄지에 대한 답을 스프린트 4(비교 모드) 전에는 내야 한다
- **두 벤더의 MID 위치 차이가 배치에서 자동으로 드러난다.** Backup Switch와
  Meter Collar 모두 서비스 바로 아래 랭크에 서고, Enphase 쪽은 컬라이너와
  마이크로인버터가 같은 줄에 하나 더 붙는다. 사람이 손대지 않아도 부품 수 차이가
  보인다 — 이것이 스프린트 4에서 표로 나올 값의 시각적 예고편이다
- **도면에 부하(load)가 없다.** 메인 패널이 말단이고 그 아래가 비어 있다.
  "백업 경계 안쪽"을 그리려면 부하 노드가 필요한데, 스키마에 부하 class가 없다.
  스프린트 2에서 백업 경계를 그리려는 순간 막힌다

## 8. 남은 결정 사항

- 부하 표현. `load`/`load_group` class를 device-library에 둘지, 토폴로지에
  사이트 컨텍스트로 붙일지. 스프린트 2 착수 전에 정해야 한다
- 백업 경계선을 어떤 어휘로 그릴지 (파선 영역? 굵기? 별도 레이어?).
  색은 이미 활선/사선에 배정되어 있어 쓸 수 없다
- `physical` 레이어는 스키마에만 있고 데이터가 없다. 렌더러는 받을 준비만 되어 있다
- 심볼의 IEEE/ANSI 관례 대조 — 룰 엔진의 NEC 조문 대조와 함께 전기 엔지니어 리뷰 대상
- 라벨의 국문/영문 혼용 기준. 현재는 topology의 `label`을 그대로 쓴다
- 비교 모드에서 캔버스 폭이 구성마다 다르다 (456 vs 672). 나란히 놓으려면 폭을
  정규화할지, 축척을 맞출지 결정해야 한다

## 현재 상태

```
devices 10 · topologies 2 · error 0 · warning 0 · info 17 · open TODO 15
tests 26 passed (검증기 9 + 렌더러 17)
render 2 SVG — tesla 456×615, enphase 672×733
```

`npm run render`는 스키마 error가 하나라도 있으면 도면을 그리지 않고 종료한다.
검증되지 않은 데이터로 그린 도면은 틀린 도면보다 나쁘다 — 그럴듯해 보이기 때문이다.
