# 스프린트 5 — 브라우저 앱 + Pages 배포 + 기여 가이드

완료 기준은 "데이터 PR만으로 제품 추가 가능"이다. 충족됐고, 테스트로 강제된다.

## 앱

`src/ui/` — Vite + React. 백엔드 없음.

CLAUDE.md §6의 3분할을 그대로 쓴다.

```
┌──────────────┬────────────────────────────┬──────────────┐
│ 구성 선택     │      SLD 캔버스 (SVG)        │  검증 결과     │
│ 벤더 · 사이트 │  레이어 토글 · 시나리오 전환    │  Finding 목록  │
└──────────────┴────────────────────────────┴──────────────┘
```

**렌더러 · 시나리오 엔진 · 룰 엔진 · 비교 엔진이 브라우저에서 그대로 돈다.**
스프린트 1~4를 순수 함수로 짠 값을 여기서 받는다. UI 전용 로직은 없다 —
`renderTopology`, `evaluateScenario`, `runRules`, `compareTopologies`를 그대로 호출한다.

`node:` 를 임포트하는 모듈은 `src/validate/load.ts` 하나뿐이고, 앱은 그걸 쓰지 않는다.
데이터는 빌드 타임에 `scripts/bundle.ts` 가 단일 JSON으로 굽는다 —
스키마 error가 하나라도 있으면 빌드가 중단된다 (§7).

### 시각 규칙 (§6)

- 캔버스는 종이다. 카드에 담지 않는다 — 그림자 · 둥근 모서리 · 테두리 없음
- **UI 크롬에는 색이 없다.** 색은 도면 안에서 활선/사선 하나만 나른다.
  Finding 심각도도 색이 아니라 좌측 선 두께와 라벨(오류/경고/정보)로 구분한다
- 모션은 토글 응답 80ms 하나뿐. 로드 시 페이드인 없음
- 타이포 1패밀리. 전체 대문자 라벨 없음. 모노스페이스는 식별자(코드, ref)에만
- 900px 미만에서 1열로 접히고 도면 높이 제한을 푼다 — 현장 참조용

### 링크가 곧 저장이다

백엔드가 없으므로 상태를 URL 해시에 담았다. 구성 · 레이어 · 시나리오 · 트립 대상 ·
사이트 조건이 전부 들어간다. "이 구성의 정전 시나리오 좀 봐줘"를 URL 하나로 보낼 수 있다.

```
#t=tesla-...,enphase-...&layers=power,comms&sc=outage_islanded&load=14.5&lra=200&util=PG%26E
```

`parseHash` / `toHash` 는 `location` 을 읽지 않는 순수 함수라 단위 테스트가 된다.

## 판단이 갈렸던 지점

### 1. 데이터를 빌드 타임에 굽는다

브라우저에서 YAML을 파싱하거나 파일을 fetch하지 않는다. 빌드 스크립트가 검증까지
끝낸 JSON 하나를 만들고, 앱은 그것만 본다. 검증 실패 시 빌드가 중단되므로
**스키마 오류가 있는 데이터는 배포될 수 없다.**

앱이 번들을 한 번 더 zod로 통과시킨다. 중복이지만 값싸고(14종), "앱은 검증되지
않은 데이터를 보지 않는다"를 코드로 보장한다.

### 2. Finding 패널에 색을 쓰지 않았다

§6은 색 규칙을 캔버스에 대해 말하지만, 패널에 빨강/노랑을 쓰면 화면 전체가
SaaS 대시보드가 된다. 심각도는 좌측 선 두께(6px/4px/2px)와 한글 라벨로 구분한다.
정보 손실은 없고 도면이 화면에서 유일한 색 주체로 남는다.

### 3. 비교 모드에 도면 높이 바닥을 뒀다

차이 요약표가 아래에서 자리를 넓히면 도면이 읽을 수 없을 만큼 줄어든다.
`min-height: 46vh` 로 바닥을 줬다. 표보다 도면이 먼저다.

## 데이터 PR만으로 제품이 추가된다

CI(`validate.yml`)가 데이터 파일만 바뀐 PR에서도 전 파이프라인을 다시 돌린다.

```
typecheck → validate → render → render --scenario all → check → compare → build → test
```

테스트가 이 성질 자체를 검사한다.

- `tests/render.test.ts` — device 파일만 추가해도 새 제품이 그려진다
- `tests/compare.test.ts` — 새 device/topology가 비교표에 그대로 나온다 (코드 변경 없음)
- `tests/validate.test.ts` — 디렉터리 파일 수와 로드 수가 일치한다 (파일이 조용히 누락되지 않는다)
- 렌더러 · 시나리오 · 룰 소스에 벤더명이나 노드 ref가 있으면 실패한다

`CONTRIBUTING.md` 에 YAML 뼈대와 함께 "모르면 null, 추정치 금지" 규칙을 적었다.

## 배포

`.github/workflows/pages.yml` — main 푸시마다 validate → test → build → Pages 배포.
`PAGES_BASE` 를 리포 이름에서 받으므로 리포명이 바뀌어도 코드는 그대로다.

`enablement: true` 를 줘서 워크플로가 스스로 Pages를 켜게 했으나, 기본 상태에서는
토큰 권한이 모자라 실패한다.

```
HttpError: Create Pages site failed.
Error: Resource not accessible by integration
```

**최초 1회 저장소 설정이 필요하다.** Settings → Pages → Source 를 GitHub Actions 로
지정하거나, Settings → Actions → General → Workflow permissions 를
Read and write 로 바꾼다.

빌드 단계(`npm ci` → `validate` → `test` → `build`)는 이 설정과 무관하게 통과한다.
막히는 것은 배포 한 단계뿐이다.

## 알려진 공백

- 반응형 회귀를 자동으로 잡을 장치가 없다. 아래 버그는 실제 기기에서 보고 나서야 드러났다.
  브라우저를 띄우는 테스트가 없으면 같은 종류가 또 나온다
- 도면 상호작용이 없다. 브레이커 클릭 조작(§4의 fault)은 좌측 select로만 된다
- 시나리오 "재생"(§6)은 상태 전환일 뿐 애니메이션이 아니다. 흐름 방향은 엔진이
  계산해 두었으나(`flows`) 아직 그리지 않는다 — 렌더러가 방향 표기를 갖고 있지 않다
- 부하 노드가 없어 `load_shed` 는 여전히 야간 아일랜딩과 같다 (스프린트 2부터의 공백)
- SolarEdge / Qcells 는 골격이라 표의 절반이 `미확인` 이다

## 좁은 화면에서 도면이 사라지던 문제

배포 후 아이폰에서 "볼 게 없다"는 보고를 받고 측정해 보니 `SVG=373x0` 이었다.

`.paper` 가 flex 컨테이너인데 `align-items` 기본값이 `stretch` 라 svg 높이를 컨테이너에
맞춰 눌렀다. 좁은 화면에서는 `.sheets` 의 높이 바닥(`min-height: 46vh`)을 풀어 두어
컨테이너 높이가 0이 되고, 도면도 0이 됐다. 오른쪽 잘림은 높이 0인 svg 안의 배경
`rect`(456px)가 삐져나온 결과였고, 별개 버그가 아니었다.

고친 것.

- `align-items: flex-start` — svg를 세로로 늘리지 않는다
- 좁은 화면은 문서 하나로 스크롤한다. 패널 세 개가 각각 스크롤되면 손가락으로 못 쓴다
- 좁은 화면에서 도면을 맨 앞에 놓는다(`order: -1`). 이 도구의 요점이 도면이다
- 식별자에 `overflow-wrap: anywhere` — `pw3.ac_out->msp.branch` 같은 문자열이
  가로 스크롤을 만든다
- 단일 보기는 높이 기준, 비교 모드는 폭 기준으로 도면 크기를 맞춘다.
  열이 좁은 비교에서 높이에 맞추면 읽을 수 없게 줄어든다

측정은 페이지에 임시 스크립트를 넣어 `document.title` 로 값을 뽑는 방식으로 했다.
**참고: headless Chrome은 창을 500px 아래로 줄이지 못한다.** 그보다 좁은 폭으로 찍은
스크린샷은 실제 렌더링과 다르다 — 처음 모바일 스크린샷이 그래서 잘려 보였다.
