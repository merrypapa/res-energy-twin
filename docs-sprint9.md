# 스프린트 9 — AI 질문 패널과 Artifact 배포

"화면에 AI 버튼을 두고, 지금 상황을 물어볼 수 있게. 그리고 그걸 내 계정에 연결해서
구독 안에서 추가 비용 없이 쓸 수 있나?"

## 답: 배포 방식이 가른다

| | GitHub Pages (지금) | Artifact (claude.ai) |
|---|---|---|
| 화면 안에서 질문 | 불가 | 가능 |
| 필요한 것 | Anthropic API 키 | 없음 |
| 비용 | 구독과 **별개**로 API 과금 | 보는 사람의 Claude 사용량에서 차감, 별도 결제 없음 |
| 키 노출 | 정적 페이지에 키를 넣으면 그대로 공개 | 키가 아예 없다 |

그래서 두 배포 모두에서 쓸모 있도록 만들었다. Artifact 안에서는 그 자리에서 묻고,
통로가 없는 배포에서는 **브리프 복사** 버튼으로 사용자가 직접 붙여넣는다.
`claude.use("sample")`이 `null`이면 기능을 접는 것이 정상 경로다 — 앱은 그것 없이도 완전히 돈다.

## 판단이 갈렸던 지점

### 1. 무엇을 넘길 것인가 — 화면이 아니라 브리프

스크린샷이나 DOM을 넘기지 않았다. `buildBrief()`가 만드는 텍스트에는 구성·옵션·제품과
정격·동작점·전력 수지·선택한 지점의 신호와 수식·룰 판정·**아직 확인되지 않은 것**이 들어간다.
전부 이미 검증을 통과한 데이터와 엔진의 계산 결과다. 브리프에 없는 것은 답에도 없어야 한다.

순수 함수라 테스트할 수 있고, 복사 버튼과 질문이 **같은 글**을 쓴다.

### 2. 답하는 쪽에 이 프로젝트의 규율을 그대로 걸었다

`ASK_INSTRUCTION`이 함께 실려 간다 — 제품 스펙을 지어내지 말 것, NEC 조항을 기억으로
인용하지 말고 브리프의 `code_ref`를 옮기며 `verified=false`면 그렇게 밝힐 것, 계산값은
어떤 가정 위의 값인지 함께 말할 것, 모르면 모른다고 할 것. CLAUDE.md §10을 프롬프트로 옮긴 셈이다.

화면에는 답이 AI가 쓴 글이라는 표기가 함께 붙는다. 검증된 판정(룰 엔진)과 생성된 글을
같은 무게로 보여주면 이 도구의 신뢰가 무너진다.

### 3. 미확인 항목의 "개수"를 세지 않기로 했다

처음에는 제품마다 `미확인 21건`처럼 숫자를 붙였는데, 그 class와 무관한 정격(서비스 인입점의
LRA 같은 것)까지 세어 숫자가 부풀었다. 무엇이 비었는지는 device의 `todos`가 훨씬 정확하게
말한다. 숫자를 빼고 todos를 넣었다.

### 4. 인코딩 — 로컬 검증이 잡은 진짜 버그

단일 HTML로 접어 열었더니 빈 화면이었다. 원인은 한글 정규식이 Latin-1로 읽혀
`Invalid regular expression: Range out of order`로 터진 것. 아티팩트 뷰어는 charset을
씌워 주지만 거기에 기대면 다른 곳에서 또 깨진다. 인라인 전에 비ASCII를 `\uXXXX`로
이스케이프해 문자셋과 무관하게 만들었다. (빌드러의 `charset: "ascii"` 옵션은 이 버전에서
번들에 반영되지 않았다.)

## 남은 것

1. Artifact는 배포 시점의 사본이다. 코드가 바뀌면 `npm run artifact` 후 다시 배포해야 한다
2. 링크를 받은 사람은 각자의 Claude 계정으로 질문한다 — 조직 정책에 따라 막힐 수 있다
3. 대화 이어가기(여러 턴)는 넣지 않았다. 지금은 한 질문 한 답이다
