# 스프린트 0 보고 — 스키마 설계에서 판단이 갈린 지점

## 1. 연결 가능 여부를 무엇으로 판정할 것인가

벤더/모델명이 아니라 **포트 타입 + 방향**으로만 판정하도록 했다 (`src/schema/compat.ts`).
호환 표에 없는 조합은 전부 불가로 떨어진다. 이렇게 하면 새 제품이 들어와도
렌더러나 룰 엔진을 건드릴 일이 없다.

대신 호환 표가 지나치게 관대해지면 검증이 무의미해진다. 현재 `ac_240v_split`과
`ac_120v_branch`를 호환으로 뒀는데, 이건 나중에 전압/상 정보를 별도 필드로 빼는 게 맞다.

## 2. 스펙 값의 nullable 처리

모든 정격을 `number | null`로 두고, **숫자가 하나라도 있으면 `sources` 필수**라는
규칙을 검증기에 넣었다 (E010). 그럴듯한 값이 데이터에 섞여 나중에 어느 게 검증된
값인지 모르게 되는 상황을 막는 게 목적이다.

`ratings`는 `.strict()`라 오타 키가 조용히 무시되지 않는다. 대신 클래스별로 의미
있는 필드가 달라서, 배터리에 `busbar_a`를 넣어도 스키마는 통과한다.
클래스별 분기는 스프린트 1에서 필요성이 확인되면 도입하는 게 낫다고 판단했다.

## 3. MID를 별도 클래스로 뺀 것

Tesla는 Backup Switch/Gateway, Enphase는 Meter Collar가 MID 역할을 한다.
장비 형태는 완전히 다르지만 **계통 분리라는 기능은 같다**. 그래서 `provides_mid`
불리언과 `mid` 클래스로 추상화했고, `requires_one_of`로 "배터리 단독으로는 백업이
성립하지 않는다"를 데이터로 표현했다.

이 추상화 덕분에 검증기가 "backup_scope가 whole_home인데 MID가 없다"(W030)를
자동으로 잡는다. 두 벤더의 차이가 자연스럽게 드러나는 지점이기도 하다.

## 4. 검증기가 실제로 잡아낸 것

첫 실행에서 error 3건이 나왔고, 전부 실제 모델링 오류였다.

- PV 모듈 ↔ 마이크로인버터 연결이 호환 표에 누락 → 표 보완
- Powerwall 3의 comms를 ethernet 하나로 뒀는데 Backup Switch는 전용 통신 →
  포트를 분리하고 "실제 매체 확인 필요" TODO 추가
- Combiner의 comms 포트 연결 수 제한이 1이라 배터리+컬러 동시 연결 불가 → 정정

스키마부터 만든 이유가 이거다. 그림부터 그렸으면 셋 다 그냥 선으로 그려졌을 것이다.

## 5. 남은 결정 사항

- PV 스트링 구성(모듈 수, 스트링 수)을 노드 `count`로 뭉갤지 별도 구조로 뺄지
- 유틸리티 승인 현황(미터 컬러)은 변동이 커서 device 파일이 아니라 확인일이 붙은
  별도 데이터로 분리해야 한다
- G4 ESS device 파일: 내부 사양 확인 후 추가. MSC/MID 구성이 `provides_mid` +
  `requires_one_of`로 표현 가능한지 검토 필요

## 현재 상태

```
devices 10 · topologies 2 · error 0 · warning 0 · info 17 · open TODO 15
tests 9 passed
```

info 17건은 대부분 "draft 상태 device 사용"이다. 제조사 매뉴얼 대조 전까지는
정상적인 상태이고, verified 승격이 곧 다음 데이터 작업이다.
