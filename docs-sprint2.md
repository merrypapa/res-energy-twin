# 스프린트 2 — 시나리오 상태 머신

`(topology, devices, scenario) => 급전 상태 + 흐름 방향`. 순수 함수 하나다.
렌더러는 한 줄도 고치지 않았다 (스프린트 1이 남긴 `energization` 인자에 그대로 물렸다).

## 판단이 갈렸던 지점

### 1. 급전 전파를 무엇으로 막을 것인가 — 포트 방향이 아니라 도메인

처음에는 포트의 `direction`(in/out/bidirectional)으로 전파를 막으려 했다. 틀렸다.

`msp.main_lugs`는 `direction: in`이다. 이 값으로 막으면 아일랜딩 시 MSP에서
Backup Switch 부하측으로 가는 도체가 사선으로 계산된다. 실제로는 활선이다 —
개방된 MID 접점 앞까지 전압이 올라와 있다. `direction`은 "정상 운전 시 전력이
들어오는 쪽"이지 "이 도체가 반대편에서 급전될 수 있는가"가 아니다.

실제로 물리를 가르는 규칙은 **AC → DC 역전파 금지**다. 야간에 인버터가 어레이를
역급전하지 않는다. 이건 포트 `type`의 접두사(`ac_` / `dc_`)로 판정되고, 벤더도
제품도 보지 않는다.

부수 효과가 좋다. 야간 아일랜딩에서

- `pv.dc_out → micro.dc_in` 은 **사선** (어레이가 죽어 있다)
- `micro.ac_out → comb.pv_ac_in` 은 **활선** (AC 도체는 밤에도 살아 있다)

두 줄이 다르게 계산된다. 현장 참조 용도에서 의미 있는 구분이다.

### 2. 그리드 포밍을 코드가 아니라 데이터로 옮겼다

"계통 없이 전압을 세울 수 있는가"를 알아야 아일랜드 형성 주체가 정해진다.
이걸 엔진에 넣으면 — `class === "hybrid_inverter_battery"면 포밍 가능` 같은 식으로 —
렌더러에 벤더 분기문을 넣는 것과 같은 종류의 오류가 된다. 클래스는 심볼을 고르는
기준이지 능력의 근거가 아니다.

`device.grid_forming` / `device.black_start_capable`을 추가했다. 둘 다 nullable이고
**null은 "불가"로 취급되며 finding을 남긴다.** 확인 전까지 성립을 주장하지 않는다.

그 결과 지금 블랙스타트는 두 구성 모두 **성립하지 않는다**고 나온다.
`black_start_capable`이 전부 null이기 때문이다. 이건 버그가 아니라 데이터 공백의
정직한 표현이다. 매뉴얼을 확인해 값을 넣는 순간 코드 변경 없이 결과가 바뀐다
(그 동작을 테스트로 고정해 뒀다).

### 3. 추종 인버터는 고정점 반복으로 처리했다

마이크로인버터는 스스로 전압을 세우지 못하고, 누가 세운 계통을 따라간다.
"살아 있으면 출력한다 → 출력하면 더 살아난다"는 순환이라 한 번의 순회로 풀리지 않는다.
소스 집합을 넓혀 가며 변화가 없을 때까지 반복한다(노드 수 + 1 회로 상한을 걸었다).

### 4. 트립 대상은 시나리오 파일에 넣지 않았다

`fault`는 "지정 브레이커"를 요구하는데, 그 ref는 토폴로지마다 다르다.
`fault.json`에 `pw3`를 적으면 그 파일이 Tesla 구성 전용이 된다.
시나리오는 `requires_trip_target: true`만 선언하고, 실제 대상은 호출자가
`evaluateScenario(..., { open: ["pw3"] })`로 넘긴다. 지정이 없으면 info를 남긴다.

### 5. 흐름의 "크기"는 모델링하지 않기로 했다

CLAUDE.md가 요구한 것은 방향과 활선/사선이다. 수출량이나 부하 분담을 계산하려면
부하 크기와 발전량이 필요하고, 그건 명시된 비목적이다. `Flow`는
`forward | reverse | none` 세 값만 갖는다. `forward`는 `edge.from → edge.to`다.

## 알려진 공백

- **부하 노드가 없다.** `load_shed`가 실제로 차단할 대상이 없어 야간 아일랜딩과
  같은 결과를 낸다. 엔진이 S030으로 이 사실을 보고한다. 부하/우선순위 모델링은
  별도 스키마 확장이 필요하다
- **`fault`로 MSP를 트립시켜도 그림이 안 바뀐다.** 하위에 분기회로가 없기 때문이다.
  위 공백과 같은 뿌리다
- `black_start_capable` 전부 null, `iq8-microinverter.grid_forming` null —
  매뉴얼 대조 대상

## 다음 스프린트로 넘기는 구조 문제

`buildRenderGraph`(토폴로지 + device → 해석된 그래프)를 시나리오 엔진이
`src/render/`에서 가져다 쓴다. 이름과 위치가 렌더 전용처럼 보이지만 실제로는
도메인 함수다. 스프린트 3의 룰 엔진도 같은 해석을 필요로 하므로, 그때
`src/graph/`로 옮기는 것이 맞다고 본다. 지금 옮기면 "렌더러 무변경"이라는
스프린트 1 계약을 흐리게 되어 미뤘다.

## 검증

`tests/scenario.test.ts` — 57개 중 34개가 이 스프린트 몫이다.

전 토폴로지 × 전 시나리오에 대한 불변식 검사를 포함한다.

- AC → DC 역급전 경로가 생기지 않는다
- 개방 노드를 관통해야만 닿는 노드는 사선이다
- 개방된 노드는 소스가 되지 않는다
- 모든 엣지가 판정을 받고, 사선 엣지에는 방향이 없다
- 같은 입력이면 같은 결과다
- 엔진 소스에 벤더명도 노드 ref도 없다

개방 노드 차단 로직을 제거해 보면 4개가 깨진다 — 테스트가 실제로 무언가를 잡는다.

## 확인 방법

```bash
npm run render -- --scenario all          # 시나리오별 도면 10장
npm run render -- --scenario fault --open pw3
```
