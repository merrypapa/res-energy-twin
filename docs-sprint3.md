# 스프린트 3 — 룰 엔진

`(topology, devices, site) => RuleFinding[]`. 순수 함수 다섯 개.

완료 기준("200A 패널 과부하 구성에서 경고 발생")은 **픽스처 없이** 충족됐다.
스프린트 0에 커밋된 Tesla 구성이 이미 120% 한도를 넘는다.

```
WARN R010  메인 서비스 패널 200A: 메인 200A + 백피드 60A = 260A 가
           버스바 200A의 120%(240A)를 초과한다
           refs: msp, pw3.ac_out->msp.branch  ·  NEC 705.12(B) 계열 — 원문 대조 필요
```

## 판단이 갈렸던 지점

### 1. 검증기와 룰 엔진의 경계

CLAUDE.md §5의 초기 체크 항목 중 "포트 타입 불일치, 필수 동반 장비 누락"은
**구현하지 않았다.** 이미 검증기에 있다(E023, E025). 옮기거나 복제할 이유가 없다.

경계를 이렇게 잡았다.

- **검증기** — 이 데이터가 말이 되는가. 참조 무결성, 포트 호환, 출처 유무
- **룰 엔진** — 이 구성이 **이 집에** 성립하는가. 사이트 조건이 있어야 답이 갈린다

같은 `Finding`을 쓰지 않고 `RuleFinding`을 따로 둔 이유도 같다.
룰 결과는 `refs`(도면에서 지목할 대상)와 `code_ref`(근거 조항)를 갖는다.

### 2. 백피드를 포트 id가 아니라 포트 type으로 식별했다

120% 룰은 "메인 OCPD + 백피드 OCPD ≤ 버스바 × 1.2"인데, 무엇이 백피드인지를
먼저 정해야 한다. `port.id === "branch"` 같은 판정은 generic MSP에만 맞고
다른 패널 제품이 들어오면 깨진다.

포트 **type**으로 가른다. `ac_240v_split` / `ac_120v_branch`에 붙은 전원은 백피드,
`ac_service_line`에 붙은 전원은 라인측 탭(R020 소관)이라 120% 합산에서 뺀다.

이 구분은 테스트로 강제된다. 라인측 100A와 부하측 60A가 섞인 픽스처에서
백피드가 60A로만 잡히는지 본다 — 필터를 지우면 이 테스트가 깨진다.

### 3. kVA를 kW로 환산하지 않았다

Enphase 구성의 전원 정격은 7.08 kVA이고 백업 부하는 kW로 주어진다.
역률을 모르는 상태에서 환산하면 없는 정보를 만드는 것이다. 섞지 않고
"단위가 달라 비교할 수 없다"고 보고한다(R030.3).

Tesla는 11.5 kW라 그대로 비교된다. 같은 룰이 구성에 따라 판정하기도 하고
보류하기도 한다 — 이게 정상이다.

### 4. 조문은 룰 파일에도 쓰지 않았다

CLAUDE.md §5의 지시대로 `code_ref: "NEC 705.12(B) 계열 — 원문 대조 필요"` 형태로만
표기하고 `verified: false`를 유지한다. 조문 문구는 어디에도 적지 않았다.
CLI가 매 실행 끝에 미대조 룰을 나열하고 "사내 배포용 근거가 아니다"를 출력한다.

`verified`가 룰 단위 기본값이고 개별 Finding이 물려받는다. 원문 대조를 마친
룰만 `true`로 바꾸면 된다.

### 5. site 컨텍스트를 위한 디렉터리를 만들지 않았다

`SiteContext`(부하 kW, 최대 모터 LRA, 유틸리티, 서비스 정격)는 토폴로지에
담기지 않는 값이다. 예제 site 파일을 `sites/`에 넣을까 하다가 두었다 —
CLAUDE.md의 디렉터리 구조에 없는 항목을 임의로 늘리지 않는 편이 낫다고 봤다.

`npm run check -- --site <file.json>`으로 넘긴다. 값이 없으면 룰은 판정하지 않고
"정보 없음"을 남긴다. 기본값을 추정해 채우지 않는다.

이건 되돌리기 쉬운 결정이다. 사이트 카탈로그가 필요해지면 그때 디렉터리를 연다.

## 룰 목록

| ID | 항목 | 근거 | 현재 결과 |
|---|---|---|---|
| R010 | 버스바 120% | NEC 705.12(B) 계열 | Tesla **경고** / Enphase 판정 불가(OCPD 미기재) |
| R020 | 라인측 탭 | NEC 705.11 계열 | 두 구성 모두 부하측 |
| R030 | 백업 부하 vs 연속 출력 | — | site 필요 |
| R040 | 모터 기동 LRA | — | site 필요 |
| R050 | 유틸리티 승인 | — | 두 구성 모두 **경고** |

## 알려진 공백

- **R030이 아일랜드 경계를 계산하지 않는다.** 전 인버터를 백업 전원으로 합산하므로
  `backup_scope: partial` 구성에서 과대평가가 된다. 시나리오 엔진의
  `outage_islanded` 결과를 받아 쓰면 정확해지지만, 룰 → 시나리오 의존을
  들이는 결정이라 미뤘다. `backup_scope`가 whole_home이 아니면 그 사실을 보고한다
- **R040은 전원 병렬 기동 분담을 모델링하지 않는다.** LRA 최대값 하나로만 본다
- Enphase 백피드 도체 OCPD 미기재 → R010이 판정 불가. 데이터 공백이지 룰 공백이 아니다

## 검증

`tests/rules.test.ts` — 전체 93개 중 36개가 이 스프린트 몫이다.

- 완료 기준(200A 과부하 경고)을 직접 고정
- 모든 finding이 refs를 갖고, refs가 실재하는 노드/엣지/토폴로지만 가리킨다
- 조문 인용 룰은 전부 `verified: false`이고 `code_ref`에 "원문 대조 필요"가 남아 있다
- 룰 하나가 던져도 나머지 판정이 나온다
- 룰 소스에 벤더명도 노드 ref도 없다

변조 검사를 두 번 돌렸다.

- `1.2 → 1.5`: 4개 실패
- 서비스 도체 필터 제거: **처음엔 93개 전부 통과했다.** 해당 테스트가 실제
  데이터에서 일어날 수 없는 경우를 보고 있어 헛돌았다. 라인측/부하측이 섞인
  픽스처로 바꾸고 나서야 잡힌다

## 확인 방법

```bash
npm run check
npm run check -- --site site.json --topology tesla-pw3-backup-switch-whole-home
```

`site.json` 형식 (전 항목 선택):

```json
{ "utility": "PG&E", "backup_load_kw": 14.5, "largest_motor_lra": 200, "service_a": 200 }
```
