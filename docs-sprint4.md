# 스프린트 4 — 4종 확장 + 비교 모드

`compareTopologies(topologies, devices, {site, scenario}) => Comparison`. 순수 함수.
`npm run compare`가 마크다운 표를 낸다.

완료 기준("벤더별 차이가 표로 자동 생성")은 충족됐다. 벤더별 예외 분기는 한 줄도 없다 —
모든 셀이 스키마에서 도출된다.

## CLAUDE.md가 예측한 것이 실제로 나왔다

> 동일 스키마로 표현되는 순간, 부품 수 / 서브패널 필요 여부 / 결선 포인트 수가
> 자동으로 비교된다. 이것이 이 프로젝트의 핵심 산출물이다. (§3.3)

| | Enphase | Tesla |
|---|---|---|
| 설치 부품 수 | 5종 | 3종 |
| 전력 결선 포인트 | 6 | 4 |
| 통신 결선 포인트 | 2 | 1 |
| 백업 서브패널 | 불필요 | 조건부 |
| 결합 방식 | AC 결합 (모듈 단위) | DC 결합 (스트링) |

`결합 방식`도 제품명이 아니라 연결 구조에서 나온다 — PV가 마이크로인버터로 가면 AC 결합,
스트링이 인버터 DC 입력으로 가면 DC 결합이다.

## 판단이 갈렸던 지점

### 1. 빈칸을 쓰지 않았다

값이 없는 셀은 `미확인`으로 채운다. 빈칸은 0으로 읽히고, 0은 "부품이 없다"거나
"용량이 없다"로 오독된다. SolarEdge/Qcells 열이 `미확인`으로 가득한 것이 지금 상태의
정확한 표현이다.

### 2. 두 벤더는 골격만 세웠다

CLAUDE.md §3.3이 준 한 줄("DC 결합, 스트링 구조" / "MSC/MID 구성")에서만 도출했고,
그 사실을 topology의 `sources`에 적었다. 수치·인증·승인 상태는 전부 null이다.

전체 백업 구성을 **가정**해 모델링했다 — 비교를 같은 조건에서 하기 위해서다.
가정이라는 사실은 `todos`에 남겼다.

검증기가 W031(MID 미확정)로, 시나리오 엔진이 S023/S021로 이 공백을 매번 보고한다.
표에도 `MID 미확정 2건 — 아일랜드 경계 비교가 성립하지 않는다`가 노트로 붙는다.

### 3. 예상 노무시간은 만들지 않았다

CLAUDE.md §6의 차이 요약 항목이지만, 노무 데이터가 없다. 결선 포인트 수가 대리 지표이나
시공 시간과 선형 관계가 아니다. 열을 만들어 추정치를 채우는 대신 노트로 남겼다.

### 4. 비교 상한을 4개로 걸었다

§6의 "캔버스 2~4분할"과 같은 수다. 5개 이상을 넣으면 CLI가 거부한다.

## 스키마 변경 두 건

두 건 다 새 벤더를 넣다가 "정직하게 표현할 수 없다"는 벽에 부딪혀 나왔다.

### `provides_mid`가 nullable이 됐다

확인되지 않은 제품에 `false`를 쓰면 "제공하지 않는다"고 단정하는 것이다.
`grid_forming` / `black_start_capable`과 같은 처리로 통일했다.

### `port.mid_side`가 생겼다

비교 테스트가 엔진 버그를 찾았다. MID를 내장한 올인원 장치가 정전 시 소스에서
통째로 빠졌다 — 노드 전체를 개방으로 처리했기 때문이다. 물리적으로는 접점만 열리고
인버터는 부하측에 계속 급전한다.

모델이 "접점의 어느 쪽에 어떤 포트가 있는지"를 표현하지 못한 것이 원인이었다.
`mid_side`(grid|load|null)를 추가했다. 표기가 있으면 그 경계로만 끊고, 없으면
보수적으로 전체를 차단하되 S024로 "백업 결과가 실제보다 작다"를 남긴다.

부수적으로 두 가지가 더 드러났다.

- **자동 개방과 지정 트립을 구분하지 않고 있었다.** 계통 상실로 열린 MID는 여전히
  출력하고, 트립된 장치는 아무것도 내보내지 않는다. 하나로 묶여 있던 것이 위 버그의
  직접 원인이다
- **`injectors`의 의미가 느슨했다.** "출력할 자격이 있다"가 아니라 "실제로 도체를
  살렸다"로 조였다. 개방 접점에 갇혀 아무것도 못 살린 장치가 아일랜드 형성 주체로
  보고되고 있었다

## 스프린트 0 테스트 하나를 고쳤다

`expect(topologies.items.length).toBe(2)` — 데이터 파일을 추가하면 깨지는 단언이다.
"데이터 파일만 고쳐서 새 제품을 추가할 수 있어야 한다"(§7)와 정면으로 배치된다.
디렉터리의 파일 수와 로드된 수가 일치하는지 보는 불변식으로 바꿨다.

## 알려진 공백

- SolarEdge/Qcells는 전 수치 null. 표의 절반이 `미확인`이다
- DC 결합 축전지를 표현할 DeviceClass가 없다. SolarEdge를 `hybrid_inverter_battery`
  한 노드로 합쳐 두었다 — 스키마 공백
- 예상 노무시간 미모델링
- UI 비교 모드(2~4분할 캔버스)는 아직 없다. 지금은 CLI 표가 그 자리다

## 확인 방법

```bash
npm run compare
npm run compare -- --scenario outage_islanded
npm run compare -- --only tesla,enphase --diffs
npm run compare -- --json
```
